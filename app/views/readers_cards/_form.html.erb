<%= form_with(model: readers_card) do |form| %>
  <% if readers_card.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(readers_card.errors.count, "error") %> prohibited this readers_card from being saved:</h2>

      <ul>
        <% readers_card.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-outline mb-4">
    <%= form.date_field :issue_date, class: "form-control form-control-lg" %>
  </div>
  <div class="form-outline mb-4">
    <%= form.text_field :number, class: "form-control form-control-lg" %>
  </div>
  <div class="form-outline mb-4">
    <label class="form-label" for="library">Library id:</label>
    <%= form.number_field :library_id, class: "form-control form-control-lg", id: "library" %>
  </div>
  <div>
    <%= form.submit "Edit", class: "btn btn-primary btn-lg btn-block w-100" %>
  </div>
<% end %>
